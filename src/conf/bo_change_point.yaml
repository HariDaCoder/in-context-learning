inherit:
  - bo_iid.yaml

# With max_context=80 there are 79 transitions: 39 IID then 40 correlated.
# The reverse config uses exactly the reversed coefficient sequence.
training:
  data_kwargs:
    rho: 0.0
    rho_after: 0.9
    change_point: 40

out_dir: ../models/bo_change_point

wandb:
  name: bo_change_0_to_09_snr2_seed0
