inherit:
  - bo_iid.yaml

# IID features with temporally correlated label noise. Marginal noise variance
# remains 1/SNR^2 for every demonstration.
training:
  task_kwargs:
    snr: 2.0
    noise_rho: 0.8

out_dir: ../models/bo_noise_markov

wandb:
  name: bo_noise_ar1_rho08_snr2_seed0

